/* O código fornecido tem  um erro de sintaxe na linha onde a função printf() é chamada.
 A aspa de fechamento está incorreta, é usada uma aspa simples (') em vez de uma aspa dupla (").*/

#include <stdio.h>
int main()
{
printf( "Hello, world!\n" );

/*printf( "Hello, world!\n’ );*/
return 0;
}
